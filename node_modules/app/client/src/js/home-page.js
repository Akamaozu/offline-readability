var React = require('react'),
    div = React.DOM.div;

module.exports = React.createClass({

  render: function(){

    return div({ id: 'logged-out' },

      require('app/client/src/js/home-landing-template'),      
      React.createElement( require('app/client/src/js/login-form-template'), {

        credentials_error: this.props.credentials_error,
        server_error: this.props.server_error
      })
    );
  }
});
