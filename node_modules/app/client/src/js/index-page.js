var React = require('react'),
    div = React.DOM.div,
    a = React.DOM.a;

module.exports = React.createClass({
  
  render: function(){

    return div({ id: 'bookmarks' },

      React.createElement( require('app/client/src/js/index-pagination-template'), { metadata: this.props.metadata } ),
      React.createElement( require('app/client/src/js/index-list-template'), { bookmarks: this.props.bookmarks } ),
      React.createElement( require('app/client/src/js/index-pagination-template'), { metadata: this.props.metadata } )
    );
  }
});